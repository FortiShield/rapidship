apiVersion: v1
kind: Service
metadata:
  name: {{ include "rapidship.fullname" . }}
  labels:
    {{- include "rapidship.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rapidship.selectorLabels" . | nindent 4 }}